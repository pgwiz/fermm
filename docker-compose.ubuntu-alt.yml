version: '2.2'

# FERMM Docker Compose for Ubuntu with Alternative Ports
# Uses ports 8080 (HTTP) and 8443 (HTTPS) to avoid conflicts
# If port 80 is already in use, use this file instead:
#   sudo docker-compose -f docker-compose.ubuntu-alt.yml up -d

services:
  fermm-server:
    image: popox15/fermm-server:latest
    container_name: fermm-server
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - FERMM_DATABASE_URL=postgresql+asyncpg://fermm:${POSTGRES_PASSWORD}@fermm-postgres:5432/fermm
      - FERMM_JWT_SECRET=${JWT_SECRET}
      - FERMM_ADMIN_USERNAME=${ADMIN_USERNAME}
      - FERMM_ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - FERMM_LOG_LEVEL=${LOG_LEVEL}
      - FERMM_DEBUG=${DEBUG}
    depends_on:
      fermm-postgres:
        condition: service_healthy
    networks:
      - fermm-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/", "||", "exit", "1"]
      interval: 10s
      timeout: 5s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  fermm-postgres:
    image: postgres:16-alpine
    container_name: fermm-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=fermm
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=fermm
      - POSTGRES_INITDB_ARGS=-c shared_buffers=256MB -c max_connections=200
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - fermm-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fermm -d fermm"]
      interval: 5s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  fermm-nginx:
    image: nginx:alpine
    container_name: fermm-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./conf.d:/etc/nginx/conf.d:ro
      - ./fermm-dashboard/dist:/usr/share/nginx/html:ro
      - certbot_certs:/etc/letsencrypt:ro
      - certbot_www:/var/www/certbot:ro
    depends_on:
      - fermm-server
    networks:
      - fermm-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Optional: SSL certificate management with Let's Encrypt
  certbot:
    image: certbot/certbot:latest
    container_name: fermm-certbot
    restart: unless-stopped
    volumes:
      - certbot_certs:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew --quiet; sleep 12h & wait $${!}; done;'"

networks:
  fermm-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  certbot_certs:
    driver: local
  certbot_www:
    driver: local
